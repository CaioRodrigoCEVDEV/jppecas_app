name: jppecas_app
description: Aplicativo Flutter que sincroniza produtos com API JP Peças

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  connectivity_plus: ^5.0.2
  url_launcher: ^6.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
